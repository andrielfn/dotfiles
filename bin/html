#!/bin/sh
#
# Create a new front-end project.
#
# USAGE:
#
#   $ html some/dir
#
if [ $# -lt 1 ]
then
  echo "Usage: $(basename $0) PATH"
  exit 1
fi

git clone git@github.com:andrielfn/frontend-boilerplate.git $1
rm -rf $1/{.git,.gitignore,LICENSE,README.md}
