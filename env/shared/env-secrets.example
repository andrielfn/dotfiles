#!/usr/bin/env bash
# =============================================================================
# SHARED ENVIRONMENT SECRETS
# =============================================================================
# Copy this file to 'env-secrets' and add your actual secret values
# This file should NOT be tracked by git
# These secrets are loaded in both personal and work environments

# Global GitHub token (if you use the same one for both)
# export GITHUB_TOKEN="your_github_token_here"

# Claude/AI API keys
export ANTHROPIC_API_KEY="your_anthropic_api_key"
export OPENAI_API_KEY="your_openai_api_key"

# Global development tools
export HOMEBREW_GITHUB_API_TOKEN="your_homebrew_github_token"

# Database credentials (development)
# Prefer using ~/.pgpass (chmod 0600) for PostgreSQL credentials instead of
# environment variables. Format: hostname:port:database:username:password
# See: https://www.postgresql.org/docs/current/libpq-pgpass.html

# GPG should prompt for passphrase interactively via pinentry.
# Do NOT store GPG_PASSPHRASE in environment variables — process env is
# visible via `ps` and defeats the purpose of passphrase protection.

# Other shared secrets that apply to both environments
# export SHARED_SECRET="your_shared_secret"
